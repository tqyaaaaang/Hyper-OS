/**
 * schedule/schedule.cpp
 * schedule processes and cpus
 */

#include "schedule.h"
#include <list>

using std::list;

static list<process_t*> running, sleeping, zombie;

void sched_init_proc(process_t *proc)
{
	
}

void sched_set_core(process_t *proc)
{
	
}

void sched_set_runable(process_t *proc)
{
	
}
